<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/primary"
    tools:context=".activities.SettingsActivity">

    <include
        android:id="@+id/toolbar"
        layout="@layout/toolbar" />

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true"
        android:layout_below="@id/toolbar">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="12dp"
            android:orientation="vertical">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/update"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:cornerRadius="16dp"
                android:padding="12dp"
                android:text="Update Profile"
                android:gravity="start"
                android:textColor="@color/white"
                android:textSize="16sp"
                app:iconGravity="end"
                app:icon="@drawable/round_person_4_24"
                app:iconTint="@color/white"
                app:backgroundTint="@color/second" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/policy"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:cornerRadius="16dp"
                android:padding="12dp"
                android:text="Privacy Policy"
                android:gravity="start"
                android:textColor="@color/white"
                android:textSize="16sp"
                app:iconGravity="end"
                app:icon="@drawable/round_privacy_tip_24"
                app:iconTint="@color/white"
                app:backgroundTint="@color/second" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/help"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:cornerRadius="16dp"
                android:padding="12dp"
                android:text="Help &amp; Support"
                android:gravity="start"
                android:textColor="@color/white"
                android:textSize="16sp"
                app:iconGravity="end"
                app:icon="@drawable/round_support_agent_24"
                app:iconTint="@color/white"
                app:backgroundTint="@color/second" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/logout"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:cornerRadius="16dp"
                android:padding="12dp"
                android:text="Logout"
                android:gravity="start"
                android:textColor="@color/cha_item"
                android:textSize="16sp"
                app:iconGravity="end"
                app:icon="@drawable/round_logout_24"
                app:iconTint="@color/cha_item"
                app:backgroundTint="@color/red" />

        </LinearLayout>

    </androidx.core.widget.NestedScrollView>


</RelativeLayout>